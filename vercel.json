{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "ignoreCommand": "echo 'Ignoring sample-data' && rm -rf sample-data",
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
} 